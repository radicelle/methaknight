---
import { Picture } from "@astrojs/image/components";
import heroImage from "assets/dreamedUnit-tr.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon";
---

<main class="grid lg:grid-cols-2 place-items-center pb-8 md:pt-16 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Site rêvé par une intelligence artificielle"
      widths={[200, 200, 400]}
      aspectRatio="4:2"
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
      onmouseover={"toto"}
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      Bienvenue sur le site web de Agrigaz
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      Agrigaz est une unité de production de biogaz située dans le bocage
      normand à Vire-Normandie. Implenté depuis 2019 dans le paysage de la
      ville, le site produit déjà l'équivalent de la consommation en gaz de
      25000 foyés chaque année.
    </p>
  </div>
</main>
